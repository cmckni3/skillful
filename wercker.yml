box: wercker/rvm
services:
  - wercker/postgresql
build:
    steps:
        - bundle-install
        - rails-database-yml
        - bundle exec rake bower:install
        - script:
            name: minitest
            code: bundle exec rake test
